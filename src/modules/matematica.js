
const PI = 3.14;


const sumar = (x, y) => x + y;
const restar = (x, y) => x - y;
const multiplicar = (x, y) => x * y;
const dividir = (x, y) => x / y;


const stringsArray = ["dos", "cuatro", "ocho", "diez"];


export { PI, sumar, restar, multiplicar, dividir, stringsArray };